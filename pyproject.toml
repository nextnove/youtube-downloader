[project]
name = "youtube-downloader"
version = "0.1.0"
description = "A simple YouTube video downloader using yt-dlp."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "yt-dlp>=2024.8.6",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.15.0",
]
